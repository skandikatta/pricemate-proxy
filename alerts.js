// alerts.js — watchlist/subscription API routes
// Stub: routes were in a local-only file that wasn't committed.
// TODO: re-implement or extract from VM backup if available.
module.exports = {
  register(app, pool) {
    // no-op until alert routes are re-added
  }
}
