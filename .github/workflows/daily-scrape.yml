yaml
name: Daily Scrape

on:
  schedule:
    - cron: "0 19 * * *"
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger scrape
        run: curl -s -m 600 "https://pricemate-proxy.onrender.com/api/scrape-all"
